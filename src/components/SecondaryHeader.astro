---
// Secondary sticky header — shows category pills when main header scrolls away
---

<!-- This component is now handled inside the main Header as a sticky nav.
     Keeping this file to avoid breaking imports, but it's intentionally empty. -->
